class Class {
  constructor({ name, capacity, supervisorId, gradeId }) {
    this.name = name;
    this.capacity = capacity;
    this.supervisorId = supervisorId; // Optional Supervisor ID
    this.gradeId = gradeId; // Mandatory Grade ID
  }
}
export default Class;
